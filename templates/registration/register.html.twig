{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
<div class="container d-flex justify-content-center align-items-center min-vh-100">
    <div class="card shadow" style="max-width: 420px; width: 100%;">
        <div class="card-body p-4">
            <div class="d-flex align-items-center justify-content-center">
                <img src="{{ asset('images/logo.png') }}" width="160px">                
            </div>
            <h4 class="text-center mb-4">
                Welcome to 
                {{ include('parts/logo-text.html.twig') }}
            </h4>
            {% if app.user %}
                <div class="alert alert-info d-flex align-items-center gap-2" role="alert">
                    <i class="bi bi-info-circle-fill"></i>
                    <div>You are logged in as {{ app.user.userIdentifier }}, <a href="{{ logout_path() }}">Logout</a></div>
                </div>
                    
            {% endif %}

            <h6 class="text-center text-secondary ">Choose registration method</h6>
            <div class="d-grid gap-2 mb-3">
                <a href="{{ path('connect_google_start') }}" class="btn btn-outline-primary d-flex align-items-center justify-content-center gap-2 py-2">
                    <i class="bi bi-google"></i> Sign up with Google
                </a>
                <a href="{{ path('connect_facebook_start') }}" class="btn btn-outline-primary d-flex align-items-center justify-content-center gap-2 py-2">
                    <i class="bi bi-facebook"></i> Sign up with Facebook
                </a>
                <a href="{{ path('connect_facebook_start') }}" class="btn btn-outline-primary d-flex align-items-center justify-content-center gap-2 py-2">
                    <i class="bi bi-envelope-fill"></i> Sign up with Email
                </a>
            </div>

            
        </div>
    </div>
</div>
{% endblock %}
